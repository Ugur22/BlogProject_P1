{% extends 'base.html.twig' %}

{% block body %}
    <h1>Add Category</h1>
    {{ form_start(form) }}
    {{ form_row(form.name) }}
    <button type="submit" class="btn btn-primary" formnovalidate>Add category</button>
    {{ form_end(form) }}
{% endblock %}